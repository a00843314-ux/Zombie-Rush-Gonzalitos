// No hereda de MonoBehaviour: vive en memoria toda la sesión.
// No necesita estar en ningún GameObject.
public static class MultiplierData
{
	public static float multiplicadorActual = 1f; // sin compra = x1
}